# =============================================================================
# Agentic Inventory Restocking Service - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# API Keys (Required - at least one LLM key needed)
# -----------------------------------------------------------------------------
# Google Generative AI (Gemini)
GOOGLE_API_KEY=your-google-api-key-for-gemini

# Groq (Llama 3)
GROQ_API_KEY=your-groq-api-key

# LLM Provider Selection (auto, primary, backup)
# auto = try Gemini first, fallback to Groq if it fails
LLM_PROVIDER=auto

# API Key for securing endpoints
API_KEY=your-secure-api-key-for-endpoints

# -----------------------------------------------------------------------------
# Security Settings
# -----------------------------------------------------------------------------
# Set to 'true' ONLY for local development (bypasses API key requirement)
DEV_MODE=false

# Auto-execute threshold (0.0-1.0). Orders above this confidence auto-execute
AUTO_EXECUTE_THRESHOLD=0.6

# Dashboard password (default: admin123)
DASHBOARD_PASSWORD=your-secure-dashboard-password

# -----------------------------------------------------------------------------
# LangSmith Tracing (Phase 5 - Observability)
# -----------------------------------------------------------------------------
# Enable for full LLM call tracing and debugging
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your-langsmith-api-key
LANGCHAIN_PROJECT=inventory-agent
# Required if using org-scoped API key (get from LangSmith Dashboard > Settings > Workspaces)
LANGSMITH_WORKSPACE_ID=your-workspace-id

# -----------------------------------------------------------------------------
# Telegram Bot (Phase 5 - Bidirectional)
# -----------------------------------------------------------------------------
# 1. Create bot via @BotFather in Telegram
# 2. Get bot token and add below
# 3. Start chat with your bot and get chat_id
# 4. Set webhook: /telegram/webhook
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
TELEGRAM_CHAT_ID=your-chat-id
TELEGRAM_BOT_USERNAME=InventoryAgentBot

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
METRICS_PORT=9090

# Slack Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
NOTIFICATION_EMAIL=alerts@yourcompany.com

# Redis Cache (optional)
REDIS_URL=redis://localhost:6379

# Rate Limiting
RATE_LIMIT_PER_MINUTE=30
